{% extends 'base.html.twig' %}

{% block title %}TypeCourse{% endblock %}

{% block body %}
</div>
<div class="container-fluid mt--7">
      <!-- Table -->
      <div class="row">
        <div class="col">
          <div class="card shadow">
            <div class="card-header border-0">
    <h1>TypeCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_course.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_course.Name }}</td>
            </tr>
            <tr>
                <th>Fields_available</th>
                <td>{{ type_course.fieldsAvailable ? type_course.fieldsAvailable|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('type_course_edit', {'id': type_course.id}) }}" class="btn btn-warning">edit</a>

    {{ include('type_course/_delete_form.html.twig') }}
    </div></div></div></div>
    <a href="{{ path('type_course_index') }}" class="btn btn-outline-default">back to list</a>
{% endblock %}
