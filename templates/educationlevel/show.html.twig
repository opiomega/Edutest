{% extends 'base.html.twig' %}

{% block title %}Educationlevel{% endblock %}

{% block body %}
</br></br>

<section class="section section-lg">
      <img src="{{asset('assetss/img/path4.png')}}" class="path">
     
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-12">
            <h1 class="text-center">Education level</h1>
            <div class="row row-grid justify-content-center">
            
            </div>
          </div>
        </div>
      </div>
    </section>
<div class="container-fluid mt--7">
      <!-- Table -->
      <div class="row">
        <div class="col-xl-8 order-xl-1">
          <div class="card shadow">
            <div class="card-header border-0">
    <h1>Education level</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ educationlevel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ educationlevel.name }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('educationlevel_edit', {'id': educationlevel.id}) }}"class="btn btn-warning">edit</a>

    {{ include('educationlevel/_delete_form.html.twig') }}
    </div></div></div></div>
    <a href="{{ path('educationlevel_index') }}" class="btn btn-outline-default float-left">back to list</a>
{% endblock %}
