{% extends 'base.html.twig' %}

{% block title %}ConfigurationEmail{% endblock %}

{% block body %}
    <h1>ConfigurationEmail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ configuration_email.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('configuration_email_index') }}">back to list</a>

    <a href="{{ path('configuration_email_edit', {'id': configuration_email.id}) }}">edit</a>

    {{ include('configuration_email/_delete_form.html.twig') }}
{% endblock %}
