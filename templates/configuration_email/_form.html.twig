{{ form_start(form) }}
    {{ tinymce_init() }}
    {{ form_widget(form) }}
    <button style="border:none;background-color:transparent;"class="redback over text-white float-right">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
