{% extends 'base.html.twig' %}

{% block title %}Hearaboutus index{% endblock %}

{% block body %}
   </br></br>
<section class="section section-lg">
      <img src="{{asset('assetss/img/path4.png')}}" class="path">
     
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-12">
            <h1 class="text-center">Hear about us</h1>
            <div class="row row-grid justify-content-center">
            
            </div>
          </div>
        </div>
      </div>
    </section>
   
 {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
          {% for message in app.flashes('delete') %}
            <div class="alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}
          {% for message in app.flashes('update') %}
            <div class="alert alert-warning" role="alert">
                {{ message }}
                </div>   
                {% endfor %}  
    <table class="table">
        <thead class="thead-light">
            
            <tr>
                
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hearaboutus in hearaboutuses %}
            <tr>
                
                <td>{{ hearaboutus.name }}</td>
                <td>
                 {#}   <a href="{{ path('hearaboutus_show', {'id': hearaboutus.id}) }}">show</a>#}
                    <a class="btn btn-neutral btn-icon btn-round" href="{{ path('hearaboutus_edit', {'id': hearaboutus.id}) }}">
                     <i class="tim-icons text-yellow icon-settings-gear-63"></i></a>edit
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('hearaboutus_new') }}">Create new</a>
     <script>
$(document).ready(function(){
  $("#myInput").on("keyup", function() {
    var value = $(this).val().toLowerCase();
    $("#myTable tr").filter(function() {
      $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
    });
  });
});
</script>

  <script>
$(document).ready( function () {
    $("#exemple").DataTable();
} );
</script>
<script type="text/javascript">
				 $(document).ready(function() {
    $('#example').DataTable( {
        "order": [[ 3, "desc" ]]
    } );
} );
	</script>
{% endblock %}
