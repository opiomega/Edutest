{% extends 'base.html.twig' %}

{% block title %}Hearaboutus{% endblock %}

{% block body %}
    <h1>Hearaboutus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hearaboutus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hearaboutus.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hearaboutus_index') }}">back to list</a>

    <a href="{{ path('hearaboutus_edit', {'id': hearaboutus.id}) }}">edit</a>

    {{ include('hearaboutus/_delete_form.html.twig') }}
{% endblock %}
