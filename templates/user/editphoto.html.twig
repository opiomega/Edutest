{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
<div class="header bg-gradient-primary pb-8 pt-5 pt-md-8"> </div> 
<div class="container-fluid mt--7">
   <div class="row">

     <div class="col-xl-8 order-xl-1">
          <div class="card bg-secondary shadow">

 <div class="card-body">
    <h1>Edit Student</h1>

 {#}  <form method="post" action="{{ path('user_editphoto', {'id': user.id}) }}">#}
{{form_start(form)}}
    {{ form_widget(form.photoFile)}}
    <button type="submit" class="btn btn-success btn-md">Update</button >
{{form_end(form)}}
    
         {#<input type="hidden" name="_token" value="{{ csrf_token('save' ~ user.id) }}">
         
   </form>#}
 
                   
    </div></div></div></div>
    
    <a href="{{ path('apprentice') }}"class="btn btn-outline-default float-left">back to list</a>
{% endblock %}
