{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
</br></br>

<section class="section section-lg">
      <img src="{{asset('assetss/img/path4.png')}}" class="path">
     
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-12">
            <h1 class="text-center">New category</h1>
            <div class="row row-grid justify-content-center">
            
            </div>
          </div>
        </div>
      </div>
    </section>
 <div class="container-fluid mt--8">
   <div class="row">

     <div class="col-xl-10 order-xl-1">
          <div class="card bg-secondary shadow">

 <div class="card-body">
    <h1>Create new Category</h1>
    {{ form_start(form) }}
    <div class="row">
                    <div class="col-lg-8">
                      <div class="form-group">
                        <label class="form-control-label" for="input-address">Category name</label>
                        {{ form_widget(form.name) }}
                      </div>
                    </div>
                  </div>
<button class="btn btn-success float-right" type="submit">{{ button_label|default('Save') }}</button>
    <a href="{{ path('category_index') }}" class="btn btn-outline-default">back to list</a>
    {{form_end(form)}}
{% endblock %}
