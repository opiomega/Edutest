{% extends 'base.html.twig'%}

{% block body %}
    {{form_start(form)}}
        {{form_row(form.content)}}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{form_end(form)}}

    {{module.content|raw}}
{% endblock %}