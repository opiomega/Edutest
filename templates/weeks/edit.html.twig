{% extends 'base.html.twig' %}

{% block title %}Edit Weeks{% endblock %}

{% block body %}
    <h1>Edit Weeks</h1>

    {{ include('weeks/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('weeks_index') }}">back to list</a>

    {{ include('weeks/_delete_form.html.twig') }}
{% endblock %}
