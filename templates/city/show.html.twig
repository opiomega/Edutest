{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
</br></br>

<section class="section section-lg">
      <img src="{{asset('assetss/img/path4.png')}}" class="path">
     
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-12">
            <h1 class="text-center">City</h1>
            <div class="row row-grid justify-content-center">
            
            </div>
          </div>
        </div>
      </div>
    </section>
<div class="container-fluid mt--7">
      <div class="row">
        <div class="col-xl-8 order-xl-1">
          <div class="card  shadow">
            <div class="card-header border-0">
              <div class="row align-items-center">
                <div class="col-8">
    <h1>City</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('city_edit', {'id': city.id}) }}" class="btn btn-warning">edit</a>
</div></div></div></div>
    {{ include('city/_delete_form.html.twig') }}

    <a href="{{ path('city_index') }}" class="btn btn-outline-default float-left">back to list</a>
{% endblock %}
