{% extends 'base.html.twig' %}

{% block title %}Events{% endblock %}

{% block body %}

<section class="section section-lg">
      <img src="{{asset('assetss/img/path4.png')}}" class="path">
     
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-12">
            <h1 class="text-center"></h1>
            <div class="row row-grid justify-content-center">
            
            </div>
          </div>
        </div>
      </div>
    </section>
    <div class="container">
    <div class="card card-plain" style="border:solid 1px;">
    <div class="card-header">
    <h1>Events</h1>
 </div>
<div class="card-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event.id }}</td>
            </tr>
        </tbody>
    </table>
</div>
<div class="card-footer">
    <a class="btn btn-primary float-right" href="{{ path('events_edit', {'id': event.id}) }}">edit</a>

    {{ include('events/_delete_form.html.twig') }}
    </div></div>
    <a class="btn"href="{{ path('events_index') }}">back to list</a>

    
{% endblock %}
