{% extends 'base.html.twig' %}

{% block title %}Courses{% endblock %}

{% block body %}
</div>
<div class="container-fluid mt--8">
   <div class="row">

     <div class="col-xl-10 order-xl-1">
          <div class="card bg-secondary shadow">
          <div>
          <a href="{{ path('courses_index') }}"class="btn btn-outline-warning float-right">back to list</a>
          </div>
    <h1>Courses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course.name }}</td>
            </tr>
        </tbody>
    </table>
<div>
    

    <a href="{{ path('courses_edit', {'id': course.id}) }}"class="btn btn-outline-default float-left">edit</a>{{ include('courses/_delete_form.html.twig') }}

    
    </div>
{% endblock %}
